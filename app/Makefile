GR = ./gradlew
CC = java
PUT = src/main/java/hexlet/code

jar:
	gradle installDist

run:
	./build/install/app/bin/app

clean:
	$(GR) clean

build:
	$(GR) build

last:
	$(GR) clean build

test:
	$(GR) test
